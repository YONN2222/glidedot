{
  "name": "glidedot",
  "private": true,
  "scripts": {
    "build": "bun --filter='*' run build",
    "dev": "docker-compose up --build",
    "lint": "bun --filter='*' run lint"
  },
  "devDependencies": {
    "typescript": "latest"
  },
  "workspaces": [
    "apps/*"
  ]
}
